use videogamecollection;

create table games(
    title varchar(250) PRIMARY KEY,
    console varchar(250),
    year int,
    developer varchar(250)
);

insert into games (title, console, year, developer) values ();
